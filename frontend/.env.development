# API base intentionally empty in dev — calls go through the Vite proxy
# defined in vite.config.ts (`/api` → `http://localhost:8000`). Setting
# this to a hard http URL would break HTTPS dev (mixed-content blocked).
VITE_API_BASE=
